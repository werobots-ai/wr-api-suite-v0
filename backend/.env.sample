# Backend environment configuration
# Copy to backend/.env.local for local development.

# Required: API key used by the OpenAI client.
OPENAI_API_KEY=""

# Secrets used to encrypt and hash stored API keys. Defaults work for local dev.
API_KEY_SECRET="local-dev-secret"
API_KEY_HASH_SECRET="local-dev-secret"

# Keycloak integration. The defaults align with the local Docker Compose stack.
KEYCLOAK_BASE_URL="http://localhost:8080"
KEYCLOAK_REALM="werobots-local"
KEYCLOAK_CLIENT_ID="wr-console"
KEYCLOAK_CLIENT_SECRET="local-dev-secret"
KEYCLOAK_ADMIN_USERNAME="admin"
KEYCLOAK_ADMIN_PASSWORD="admin"

# Optional: cache behaviour tuning.
CACHE_SPEED_RATIO=""
OPENAI_CACHE_TTL_SECONDS="21600"

# DynamoDB configuration for local and cloud deployments.
AWS_REGION="us-east-1"
DYNAMODB_ENDPOINT="http://localhost:8000"
IDENTITY_TABLE_NAME="wr-api-suite-identity"
QUESTION_SETS_TABLE_NAME="wr-api-suite-question-sets"
QUESTION_SETS_SNIPPET_GSI_NAME="questionSetsBySnippet"
OPENAI_CACHE_TABLE_NAME="wr-api-suite-openai-cache"

# Optional: mark organization IDs whose usage should not be masked in responses.
WEROBOTS_INTERNAL_ORG_IDS=""

# Optional: disable dev sound effects from the caching helper by setting to 0 or false.
PLAY_SOUNDS="0"

# Optional: change the port Express listens on.
PORT=4000
