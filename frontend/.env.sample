# Frontend environment configuration
# Copy to frontend/.env.local to point the web app at a backend instance.

# Base URL for the backend API. Default assumes both apps run locally via dev.sh.
NEXT_PUBLIC_API_URL="http://localhost:4000"
